- Preferred package manager: pnpm
- Use pnpm for all installs and script runs (avoid npm/yarn).
- When adding dependencies, use `pnpm add` (or `pnpm add -D` for dev).
- Use `pnpm dlx` instead of `npx`.

